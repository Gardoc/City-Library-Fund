<Window x:Class="CityLibraryFund.WPF.Windows.EditionReadersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Читатели издания"
        Height="620"
        Width="1000"
        MinHeight="450"
        MinWidth="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundColor}">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="Читатели, у которых находится издание"
                       Style="{StaticResource HeaderText}"/>

            <TextBlock x:Name="EditionInfoTextBlock"
                       Margin="0,-12,0,0"
                       Style="{StaticResource BodyText}"
                       TextWrapping="Wrap"/>

        </StackPanel>

        <DataGrid x:Name="ReadersDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Читатель"
                                    Binding="{Binding ReaderName}"
                                    Width="*"/>

                <DataGridTextColumn Header="Тип читателя"
                                    Binding="{Binding ReaderType}"
                                    Width="180"/>

                <DataGridTextColumn Header="Инв. номер"
                                    Binding="{Binding InventoryNumber}"
                                    Width="140"/>

                <DataGridTextColumn Header="Дата выдачи"
                                    Binding="{Binding IssueDate}"
                                    Width="120"/>

                <DataGridTextColumn Header="Срок возврата"
                                    Binding="{Binding DueDate}"
                                    Width="130"/>

                <DataGridTextColumn Header="Дней осталось"
                                    Binding="{Binding DaysLeft}"
                                    Width="120"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>