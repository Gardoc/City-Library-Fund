<Window x:Class="CityLibraryFund.WPF.Windows.HallEmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Библиотекари зала"
        Height="600"
        Width="900"
        MinHeight="400"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundColor}">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="Библиотекари читального зала"
                       Style="{StaticResource HeaderText}"/>

            <TextBlock x:Name="HallNameTextBlock"
                       Margin="0,-12,0,0"
                       Style="{StaticResource BodyText}"/>

        </StackPanel>

        <DataGrid x:Name="EmployeesDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="ФИО"
                                    Binding="{Binding FullName}"
                                    Width="*"/>

                <DataGridTextColumn Header="Должность"
                                    Binding="{Binding Position}"
                                    Width="240"/>

                <DataGridTextColumn Header="Дата приема"
                                    Binding="{Binding HireDate, StringFormat=dd.MM.yyyy}"
                                    Width="160"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>